import 'package:flutter/material.dart';

class Explore_Plus extends StatelessWidget {
  const Explore_Plus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Welcome in Explore Plus"),
    );
  }
}
